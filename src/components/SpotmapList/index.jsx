// import { useContext } from 'react';
// import AppContext from '../../store/context';

export default function SpotmapList() {
  // const { state } = useContext(AppContext);
  // const { limit, page, library } = state;
  // page 1: 0 - 4
  // page 2: 5 - 9
  // page 3: 10 - 14
  // ((page - 1) * limit) = start
  // start + (limit - 1) = end
  return <div />;
}
